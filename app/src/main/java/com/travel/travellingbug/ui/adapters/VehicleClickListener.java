package com.travel.travellingbug.ui.adapters;

import com.travel.travellingbug.models.VehicelModel;

public interface VehicleClickListener {
    void onClick(int position, VehicelModel vehicelModel);
}
